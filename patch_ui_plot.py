with open("src/lib/ui_plot.py", "r") as f:
    content = f.read()

# Instead of changing every plot_line, we can just change plot_line, plot_hline, plot_marker, plot_amp_width?
# Wait, "don't display any output at all for it". This refers to the recording's output graphs.
