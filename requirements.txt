# Runtime dependencies are managed in pyproject.toml and locked via uv.lock.
# Use `uv sync` to install the full environment.
#
# This file is kept for reference / legacy tooling only.
# Do NOT add new dependencies here — add them to pyproject.toml instead.

#python==3.12
pyqt5
scipy-openblas64
pandas
joblib
matplotlib
numpy
scikit-learn==1.7.1
scipy
seaborn
tqdm
pyyaml
toml
requests
pyabf
igor2
pyarrow
